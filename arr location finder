#include<stdio.h>
#define SIZE 5
int main()
{   
    int i=0 ,j=0, arr[SIZE], arr2[SIZE];
	
	for(i=0;i<=SIZE-1;i++)
	{
	scanf("%d",&arr[i]);
}
	for(i=SIZE-1;i>=0;i--)
	{
			printf("%d\t",arr[i]);
			arr[j]=arr[i];
			j++;
	}	
	prinf("%d = %d",arr[SIZE],arr2[SIZE]);
}
